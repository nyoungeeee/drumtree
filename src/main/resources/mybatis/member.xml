<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nyoung.drumtree.MemberMapper">
	
	<!-- 멤버 리스트(검색/전체) -->
	<select id="selectMember" parameterType="java.util.Map"
		resultType="com.nyoung.drumtree.dto.MemberDTO">
		select memberIdx, memberID, memberPW, memberName, memo from test
		<where>
			<if test="memberID != null">
				memberID like '%${memberID}%'
			</if>
			<if test="memberName != null">
				and memberName like '%${memberName}%'
			</if>
			<if test="memo != null">
				and memo like '%${memo}%'
			</if>
		</where>
	</select>
	
	<!-- 회원 등록 -->
	<insert id="signIn" parameterType="java.util.Map">
		INSERT INTO test (memberName, memberID, memberPW, memo)
		VALUES (#{memberName}, #{memberID}, #{memberPW}, #{memo});
	</insert>

</mapper>